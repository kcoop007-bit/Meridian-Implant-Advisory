# Netlify configuration for meridianimplantadvisory.com
# (This site is served from the Website/ folder.)

[build]
  publish = "."
  functions = "netlify/functions"

# Reminder dispatcher is DISABLED (off until further notice) to save Netlify
# credits. To re-enable: uncomment the block below AND restore the schedule
# export in reminder-dispatch.mjs.
# [functions."reminder-dispatch"]
#   schedule = "@hourly"
